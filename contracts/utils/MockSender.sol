pragma solidity ^0.8.17;
import "./IMockExchange.sol";
import "./mockToken.sol";

contract Sender {
constructor(address governanceAddress,
        address bankAddress,
        address airdropAddress, uint initialSupply) GLDToken(initialSupply)

{
  

}


function ownerAddress() public view returns(address) {
    return address(this);
}

function issueAuction() public view returns(address) {


    
}





}